import type { App } from '../app.js';
import type { ServerOptions, UserAgentOptions } from '../types.js';
import { Server } from '../server.js';
import { UserAgent } from '../user-agent.js';
/**
 * Mock user agent class.
 */
export declare class MockUserAgent extends UserAgent {
    /**
     * Server to use for mock requests.
     */
    server: Server | undefined;
    constructor(options?: UserAgentOptions);
    /**
     * Create a new mock user agent.
     */
    static newMockUserAgent(app: App, options?: UserAgentOptions, serverOptions?: ServerOptions): Promise<MockUserAgent>;
    /**
     * Start mock server.
     */
    start(app: App, options?: ServerOptions & {
        https?: boolean;
    }): Promise<this>;
    /**
     * Stop mock server.
     */
    stop(): Promise<void>;
}
