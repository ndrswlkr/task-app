import type { App } from '../app.js';
/**
 * Minimal application generator command.
 */
declare function createFullAppCommand(app: App, args: string[]): Promise<void>;
declare namespace createFullAppCommand {
    var hidden: boolean;
    var description: string;
    var usage: string;
}
export default createFullAppCommand;
