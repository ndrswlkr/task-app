import { loadConfig } from './json-config.js';
import yaml from 'js-yaml';
/**
 * YAML config plugin.
 */
export default function yamlConfigPlugin(app, options) {
    if (options.ext === undefined)
        options.ext = 'yml';
    loadConfig(app, options, yaml.load);
}
//# sourceMappingURL=yaml-config.js.map